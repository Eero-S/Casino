package kasinoPackage
import scala.collection.mutable.Buffer

class AI(hand: Buffer[Card], board: Board, name: String) extends Player(hand, board, name){
  

  def bestMove = {
   val a = hand.map(this.inspectAction(_))
  }
  
  
}